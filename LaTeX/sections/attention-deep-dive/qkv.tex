\section{Query, Key, Value (QKV)}
\begin{frame}{Query, Key, Value (QKV)}
  \begin{itemize}
    \item[$\blacktriangleright$] \textbf{Query:} What you want (the search)
    \item[$\blacktriangleright$] \textbf{Key:} Index of content (where to look)
    \item[$\blacktriangleright$] \textbf{Value:} Actual content (what you get)
  \end{itemize}
  \vspace{1em}
  \textit{Intuition}
  \begin{itemize}
    \item[] Think of QKV like a search engine:
    \begin{itemize}
      \item[$\blacktriangleright$] \textbf{Query (Q):} Comes from the current decoder state (or token)
      \item[$\blacktriangleright$] \textbf{Key (K)}, \textbf{Value (V):} Come from the encoder (or input sequence)
    \end{itemize}
  \end{itemize}
\end{frame}

% Slide 22
\begin{frame}{QKV -- Formulation}
\textbf{Given:}
\begin{itemize}
    \item[$\blacktriangleright$] Input sequence $\mathbf{X}$
\end{itemize}
\vspace{0.5em}
\textbf{Linear projections:}
\begin{align*}
    \mathbf{Q} &= \mathbf{X} W_Q \\
    \mathbf{K} &= \mathbf{X} W_K \\
    \mathbf{V} &= \mathbf{X} W_V \\
\end{align*}

\vspace{0.5em}
\textbf{Attention:} score $\mathbf{Q}$ against $\mathbf{K}$ with the scaled dot-product alignment, softmax the scores, and take the weighted sum of $\mathbf{V}$.
\end{frame}

% Slide 23
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_qkv_formula_transformer.png}
\end{frame}

% Slide 24
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_qkv_formula_query_key_value.png}
\end{frame}

% Slide 25
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_database_key_value_store.png}
\end{frame}

% Slide 26
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_database_query_lookup.png}
\end{frame}

% Slide 27
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_database_query_matches.png}
\end{frame}

% Slide 28
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_database_two_queries.png}
\end{frame}

% Slide 29
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_database_two_matches_highlighted.png}
\end{frame}

% Slide 30
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_database_parallel_lookup.png}\end{frame}

% Slide 31
\begin{frame}{Query, Key, Value (QKV)}
  \centering
  \includegraphics[width=0.7\paperwidth,height=0.7\paperheight,keepaspectratio]{images/attention-deep-dive/attention_qkv_roles_summary.png}\end{frame}
