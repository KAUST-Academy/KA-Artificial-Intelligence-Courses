\subsection{ResNet}
\begin{frame}{}
    \LARGE CNN Architectures: \textbf{ResNet}
\end{frame}

\begin{frame}{ResNet: Motivation}
    \begin{itemize}
        \item \textbf{Problem:} Making networks deeper does not always improve accuracy.
        \item \textbf{Why?} Deeper plain networks are harder to optimize, even though batch normalization keeps gradients from vanishing. He et al.\ call this the \textbf{degradation problem}.
        \item \textbf{Solution:} Residual Network (ResNet) introduces \textbf{skip connections (residuals)}, allowing information to flow more easily.
    \end{itemize}
\end{frame}

\begin{frame}{ResNet}
    \begin{itemize}
        \item Very deep networks using residual connections
        \item 152-layer model for ImageNet
        \item Stacked Residual Blocks
        \item \textbf{Residual:} A shortcut connection that helps the network pass information through layers more easily.
        \item Won ILSVRC 2015 with 3.57\% top-5 error (ensemble).
    \end{itemize}

    \begin{figure}
        \centering
        \includegraphics[width=1.0\textwidth,height=0.38\textheight,keepaspectratio]{images/cnn/resnet_1.png}
        \caption*{\scriptsize ResNet-34. Figure: Stanford CS231n, Lecture~9, after He et al., 2015, Fig.~3.}
    \end{figure}
\end{frame}

\begin{frame}{ResNet}
    \begin{itemize}
        \item What happens when we continue stacking deeper layers on a ``plain'' convolutional neural network?
    \end{itemize}
\pause
    \begin{figure}
        \centering
        \includegraphics[width=1.0\textwidth,height=0.5\textheight,keepaspectratio]{images/cnn/resnet_2.png}
        \caption*{\scriptsize Test error (left) and training error (right) on CIFAR-10. Figure: Stanford CS231n, Lecture~9, after He et al., 2015, Fig.~1.}
    \end{figure}

\pause
    \begin{itemize}
        \item 56-layer model shows higher error on both training and test sets
        \pause
        \item The deeper model performs worse, but it’s not caused by overfitting!
    \end{itemize}
\end{frame}


\begin{frame}{ResNet}
    \begin{itemize}
        \item \textbf{Fact:} Deep models have more representation power (more parameters) than shallower models.
        \pause
        \item \textbf{Hypothesis:} The problem is an optimization problem: deeper models are harder to optimize.
        \pause
        \item \textbf{Solution:} Use network layers to fit a residual mapping instead of directly trying to fit a desired underlying mapping
    \end{itemize}
\pause
    \begin{figure}
        \centering
        \includegraphics[width=1.0\textwidth,height=0.4\textheight,keepaspectratio]{images/cnn/resnet_3.png}
        \caption*{\scriptsize Plain layers vs.\ a residual block. Figure: Stanford CS231n, Lecture~9, after He et al., 2015, Fig.~2.}
    \end{figure}
\end{frame}