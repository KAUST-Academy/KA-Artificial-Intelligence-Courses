\begin{frame}{}
    \LARGE Advanced Diffusion Models: \textbf{Text-Conditioned}
\end{frame}

\begin{frame}[allowframebreaks]{Text-Conditioned Diffusion Models}
    \begin{figure}
        \centering
        \includegraphics[width=0.5,height=0.5,keepaspectratio]{images/adv-img-gen/conditioning.png}
    \end{figure}
    \framebreak
\begin{itemize}
    \item \textbf{Overview:} Text-conditioned diffusion models enable the generation of highly specific images from textual prompts, such as \emph{"a cat wearing sunglasses standing in Times Square"}. These models leverage powerful text encoders, such as CLIP or transformer-based architectures, to extract semantic representations from the input text.

    \item \textbf{Text Conditioning:}
    \begin{itemize}
        \item The text encoder maps the prompt $y$ to an embedding $e_y = \mathrm{TextEncoder}(y)$.
        \item This embedding is injected into the denoising UNet via cross-attention layers (the mechanism is detailed in the Stable Diffusion lecture).
    \end{itemize}

    \item \textbf{The only change to the diffusion recipe:} everything from the Diffusion Models lecture carries over; the noise predictor simply takes the text embedding as an extra input, $\epsilon_\theta(x_t, t, e_y)$, in both the training objective and every denoising step.

    \item \textbf{Effect of Conditioning:} By conditioning the denoising process on $e_y$, the model generates images that faithfully reflect the textual description. The cross-attention mechanism in the UNet ensures that relevant parts of the text influence corresponding regions in the image, enabling complex and coherent scene synthesis.
\end{itemize}
\end{frame}

% GLIDE / DALL-E 2 / Imagen application showcases are taught in the Diffusion
% Models lecture (14) and the Stable Diffusion lineage (16); the duplicated
% inputs that lived here were removed.
