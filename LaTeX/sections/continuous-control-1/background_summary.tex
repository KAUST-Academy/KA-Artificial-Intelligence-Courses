\begin{frame}{Background Summary}
\begin{itemize}
    \item \textbf{DPG:} Formulates an update rule for deterministic policies, so that we can learn deterministic policy on continuous action domain
\end{itemize}
\end{frame}

\begin{frame}{Background Summary}
\begin{itemize}
    \item \textbf{DPG:} Formulates an update rule for deterministic policies, so that we can learn deterministic policy on continuous action domain
    \vspace{0.5em}
    \item \textbf{DQN:} Enables learning value functions with neural nets, with two tricks:
    \begin{itemize}
        \item Target Network
        \item Replay Buffer
    \end{itemize}
\end{itemize}
\end{frame}

\begin{frame}{Background Summary}
\begin{itemize}
    \item \textbf{DPG:} Formulates an update rule for deterministic policies, so that we can learn deterministic policy on continuous action domain
    \vspace{0.3em}
    \\ \textcolor{red}{Model-Free, Actor-Critic}
    \vspace{0.5em}
    \item \textbf{DQN:} Enables learning value functions with neural nets, with two tricks:
    \begin{itemize}
        \item Target Network
        \item Replay Buffer -- \textcolor{red}{Off-Policy}
    \end{itemize}
\end{itemize}
\end{frame}

\begin{frame}{Background Summary}
\begin{itemize}
    \item \textbf{DPG:} Formulates an update rule for deterministic policies, so that we can learn deterministic policy on continuous action domain
    \vspace{0.3em}
    \\ \textcolor{red}{Model-Free, Actor-Critic}
    \vspace{0.5em}
    \item \textbf{DQN:} Enables learning value functions with neural nets, with two tricks:
    \begin{itemize}
        \item Target Network
        \item Replay Buffer -- \textcolor{red}{Off-Policy}
    \end{itemize}
    \vspace{0.5em}
    \item \textbf{DDPG:} Learn both the policy and the value function in DPG with neural networks, with DQN tricks!
\end{itemize}
\end{frame}
