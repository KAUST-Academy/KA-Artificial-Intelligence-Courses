\section{Normalization}

\begin{frame}{The Transformer Block and LayerNorm Placement (Recap)}
  \begin{columns}[T,onlytextwidth]
    \column{0.40\textwidth}
    \textbf{The 2017 block:}
    \begin{itemize}
      \item[\textcolor{red}{$\blacktriangleright$}] Multi-head attention
      \item[\textcolor{red}{$\blacktriangleright$}] Add \& Norm
      \item[\textcolor{red}{$\blacktriangleright$}] Feed-forward layer
      \item[\textcolor{red}{$\blacktriangleright$}] Add \& Norm
    \end{itemize}
    \vspace{0.5em}
    {\footnotesize Post-LN in the original paper: normalization applied after each sublayer.}

    \column{0.56\textwidth}
    \footnotesize
    \begin{table}[ht]
      \centering
      \begin{tabular}{lcc}
        \toprule
        \textbf{Feature} & \textbf{Pre-LN} & \textbf{Post-LN} \\
        \midrule
        Stability & \checkmark\,More stable & \ding{55}\,Less stable \\
        Gradient flow & \checkmark\,Better & \ding{55}\,Can explode/vanish \\
        Used in & GPT-3, T5 & Original Transformer \\
        \bottomrule
      \end{tabular}
    \end{table}
    \vspace{0.5em}
    {\footnotesize By 2020 nearly every large model had moved the norm before the sublayer (Pre-LN): gradients reach early layers directly through the residual path, so deep stacks train without warm-up tricks.}
  \end{columns}
\end{frame}

\begin{frame}[allowframebreaks]{2026: From LayerNorm to RMSNorm}
  \textbf{LayerNorm} (Ba et al., 2016; used in the 2017 Transformer)\textbf{:} center, then scale.
  \[
    \mathrm{LN}(x) = \frac{x - \mu}{\sigma} \odot \gamma + \beta
  \]

  \textbf{RMSNorm} (Zhang \& Sennrich, 2019)\textbf{:} drop the mean subtraction and the bias; scale by the root mean square alone.
  \[
    \mathrm{RMSNorm}(x) = \frac{x}{\mathrm{RMS}(x)} \odot \gamma,
    \qquad
    \mathrm{RMS}(x) = \sqrt{\tfrac{1}{d}\textstyle\sum_{i=1}^{d} x_i^2}
  \]
  \framebreak 
  \vspace{0.5em}
  \begin{itemize}
    \item[$\blacktriangleright$] One reduction over the hidden dimension instead of two, and no bias parameters: cheaper at every layer of every token.
    \item[$\blacktriangleright$] Quality matches or slightly beats LayerNorm at scale, so centering adds cost without benefit.
    \item[$\blacktriangleright$] LLaMA, Mistral, and Qwen all combine Pre-LN placement with RMSNorm, the default in 2026.

    \item[$\blacktriangleright$] Newer stacks also normalize inside attention: Qwen3, Gemma 3, and OLMo 2 apply RMSNorm to queries and keys (QK-Norm) to keep attention logits stable.
  \end{itemize}
\end{frame}
