\section{RNN Training Challenges}
\begin{frame}{RNN Training Challenges}
  \LARGE RNN : \textbf{Challenges}
\end{frame}

\begin{frame}{RNN Basic Structure}
  \centering
  \includegraphics[width=0.9\paperwidth,height=0.95\paperheight,keepaspectratio]{images/rnn-lstm-gru/rnn_long_sequence_goal_prediction.png}
\end{frame}

\begin{frame}{RNN Training Challenges}
  \textbf{Backpropagation Through Time (BPTT)}
  \begin{itemize}
    \item Training RNNs involves unfolding the network across time steps.
    \item Standard backpropagation is applied through this unrolled structure.
    \end{itemize}
  \end{frame}

\begin{frame}{RNN: Backpropagation through time}
  \centering
  \includegraphics[width=0.9\paperwidth,height=0.95\paperheight,keepaspectratio]{images/rnn-lstm-gru/rnn_bptt_forward_backward.png}
\end{frame}

\begin{frame}{Backpropagation through time}
  \centering
  \includegraphics[width=0.9\paperwidth,height=0.95\paperheight,keepaspectratio]{images/rnn-lstm-gru/rnn_gradient_product_chain.png}
\end{frame}

\begin{frame}{Backpropagation through time}
  \centering
  \includegraphics[width=0.9\paperwidth,height=0.95\paperheight,keepaspectratio]{images/rnn-lstm-gru/rnn_gradient_distance_dependence.png}
\end{frame}

\begin{frame}{Backpropagation through time}
  \centering
  \includegraphics[width=0.9\paperwidth,height=0.95\paperheight,keepaspectratio]{images/rnn-lstm-gru/rnn_vanishing_exploding_gradient.png}
\end{frame}

\begin{frame}{RNN Training Challenges}
  \textbf{Problems}
  \begin{itemize}
    \item \textbf{Vanishing Gradients:} Gradients shrink as they are propagated back, making it hard to learn long-term dependencies.
    \item \textbf{Exploding Gradients:} Gradients grow exponentially, leading to unstable updates.
    \end{itemize}
    \textbf{Solutions Preview (covered in future modules)}
    \begin{itemize}
      \item Use of LSTM and GRU architectures
      \item Gradient Clipping
      \end{itemize}
\end{frame}

\begin{frame}{Teacher Forcing}
  \textbf{The problem when training generative RNNs}
  \begin{itemize}
    \item A generating RNN predicts token $y_t$ from its own previous output $\hat{y}_{t-1}$.
    \item Early in training the model's outputs are mostly wrong, so errors feed back as inputs and compound across the sequence.
  \end{itemize}
  \vspace{0.5em}
  \textbf{Teacher forcing}
  \begin{itemize}
    \item During training, feed the ground-truth token $y_{t-1}$ as the input at step $t$, regardless of what the model predicted.
    \item Every step is trained on a correct prefix, so learning is faster and more stable.
    \item At inference there is no ground truth: the model must consume its own predictions.
  \end{itemize}
\end{frame}

\begin{frame}{Exposure Bias and Scheduled Sampling}
  \begin{itemize}
    \item \textbf{Exposure bias:} with teacher forcing, the model is trained only on ground-truth prefixes but tested on its own imperfect prefixes. One early mistake at inference can push it into states it never saw during training.
    \item \textbf{Scheduled sampling} (Bengio et al., 2015): during training, feed the model its own prediction instead of the ground truth with some probability, and increase that probability over the course of training.
    \item The model gradually learns to recover from its own mistakes while keeping the stability of teacher forcing early on.
  \end{itemize}
\end{frame}
